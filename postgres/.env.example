# PostgreSQL Admin Configuration
POSTGRES_USER=admin
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=basecard
POSTGRES_DATA_DIR=/data2/postgres

# ============================================
# Service Users (used by init_db.sh)
# ============================================
# BaseCard Backend (uses 'public' schema)
BACKEND_USER=basecard_user
BACKEND_PASSWORD=basecard_password

# AI Agent Service (uses 'ai_agent' schema)
AI_AGENT_USER=ai_agent_user
AI_AGENT_PASSWORD=ai_agent_password

# ============================================
# Dev Database (for testnet testing)
# ============================================
# Set ENABLE_DEV_DB=true to auto-create dev database on container init
ENABLE_DEV_DB=false
DEV_DB_NAME=basecard_dev

# Container name (for scripts like setup-dev-db.sh)
CONTAINER_NAME=postgres

# ============================================
# Connection URLs
# ============================================
# Production:
#   Backend:   postgresql://basecard_user:basecard_password@localhost:5432/basecard
#   AI Agent:  postgresql://ai_agent_user:ai_agent_password@localhost:5432/basecard?schema=ai_agent
#
# Development (testnet):
#   Backend:   postgresql://basecard_user:basecard_password@localhost:5432/basecard_dev
#   AI Agent:  postgresql://ai_agent_user:ai_agent_password@localhost:5432/basecard_dev?schema=ai_agent
